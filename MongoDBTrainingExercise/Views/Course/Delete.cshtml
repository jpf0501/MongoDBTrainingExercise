@model MongoDBTrainingExercise.Models.ViewModels.CourseViewModel;

@{
    ViewData["Title"] = "Delete Course";
}

<div class="text-left">
    <h2 class="display-4 my-4">Delete Course Entry</h2>
</div>

    <p>Would you like to delete this entry?</p>
<div class="d-flex">
        @using (Html.BeginForm("Delete", "Course"))
        {
            <button class="btn btn-success">Delete</button>
        }
        <a href="@Url.Action("Index", "Course")" class="btn btn-primary mx-2">Go Back</a>
    </div>
    
